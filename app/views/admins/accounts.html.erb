<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class=""><%= link_to 'DASHBOARD', admins_path, method: :get %></li>
        <li class=""><%= link_to 'STUDENTS', students_admins_path, method: :get %></li>
        <li><%= link_to 'EVENTS', events_path, method: :get %></li>
        <li class="active"><%= link_to 'ACCOUNTS', accounts_admins_path, method: :get %></li>
        <li><%= link_to "LOG OUT", destroy_admin_session_path, :method => :delete %></a>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<div class="container with-fixed-navbar admins">

  <div class="row">
    <div class="col-xs-12">
      <h4 class="center-align">Number of accounts: <span class="text-bold"><%=@account_number %></span>/<%= Student.count %></h4>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-12">
      <table class="table-striped table">
        <tr>
          <th class="right-align">Student ID</th>
          <th>Name</th>
          <th>Email</th>
          <th class="right-align">Year</th>
          <th>Course</th>
          <th>School</th>
        </tr>
        <% @accounts.each do |student| %> 
          <tr>
            <td class="right-align"><%= student.student_id %></td>
            <td><%= student.name %></td>
            <td><%= student.email %></td>
            <td class="right-align"><%= student.yr %></td>
            <td><%= student.course %></td>
            <td><%= student.school %></td>
          </tr>
        <% end %>
      </table>
      
    </div>
  </div>
  <div class="row">
    <div class="flex col-xs-12"><%= will_paginate @accounts %></div>
  </div>
</div>
