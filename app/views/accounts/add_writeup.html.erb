<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to 'HOME', root_path, method: :get %></li>
        <li class=""><%= link_to 'ACCOUNT', accounts_path, method: :get %></li>
        <li class=""><%= link_to 'PHOTOSHOOT SIGN UPS', sign_ups_accounts_path, method: :get %></li>
        <li class=""><%= link_to 'GROUPSHOOT SIGN UPS', group_signups_accounts_path, method: :get %></li>
        <li><%= link_to "LOG OUT", destroy_account_session_path, :method => :delete %></a>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<div class="container with-fixed-navbar">
  <div class="row">
    <div class="col-xs-10 col-xs-offset-1 col-md-offset-2">
      <h1>Submit Writeup</h1>
      <h5>Writeups should not exceed more than 500 CWS (Characters with Spaces)</h5>
    </div>
  </div>
  <br>
  <div class="row">
    <%= simple_form_for @account do |f| %>
        <% if @account.errors.any? %>
          <% @account.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        <% end %>
        <div class="field col-xs-offset-2 col-xs-6">
          <%= f.text_area :writeup, cols: "120", rows: "5", :class => "writeup", :maxlength => 500 %>
          <p class="cws"></p>
        </div>
        <div class="actions col-xs-offset-4 col-xs-6">
          <%= f.submit :class => "writeup-submit", :name => "writeup_submit", :value => "Save Writeup" %>
          <%= button_to "Back to View Writeup", view_writeup_accounts_path, :name => "writeup_back", method: :get%>
        </div>
    <% end %>
  </div>
</div>